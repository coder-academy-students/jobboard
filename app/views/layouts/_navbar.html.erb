<nav class="navbar navbar-light bg-faded navbar-fixed-top">
  <div class="container">
  <a class="navbar-brand" href="/">Coder Factory Job Board</a>
  <ul class="nav navbar-nav pull-xs-right">
    <% if user_signed_in? %>
      <% if current_user.has_role? :company %>
        <li class="nav-item"><%= link_to icon('dashboard') + ' Dashboard', dashboard_path, class: 'nav-link' %></li>
      <% end %>
    <% else %>
      <li class="nav-item">
        <a class="nav-link btn btn-success-outline" href="/companies/new">Register your company</a>
      </li>
    <% end %>
    <li class="nav-item">
      <a class="nav-link" href="/jobs"><%= icon('briefcase') %> All jobs</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="/companies"><%= icon('building') %> Companies</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="/about"><%= icon('question-circle') %> About</a>
    </li>
    <% if user_signed_in? %>
      <li class="nav-item"><%= link_to icon('sign-out') + 'Sign out', destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
      <% if current_user.has_role? :admin %>
        <li class="nav-item"><%= link_to 'Admin', admin_path, class: 'nav-link' %></li>
      <% end %>

    <% else %>
      <li class="nav-item"><%= link_to 'Sign in / Register', new_user_session_path, class: 'nav-link' %></li>
    <% end %>
  </ul>
  </div>
</nav>
